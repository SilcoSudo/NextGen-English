# Production Configuration for NextGen English
# Server Configuration
PORT=5000
NODE_ENV=production

# JWT Configuration - CHANGE THESE IN PRODUCTION!
JWT_SECRET=nextgen_english_production_super_secret_key_2025_change_me_in_production
JWT_EXPIRE=7d

# CORS Configuration
FRONTEND_URL=https://nextgenenglish.id.vn
ALLOWED_ORIGINS=https://nextgenenglish.id.vn,https://www.nextgenenglish.id.vn

# Rate Limiting (more restrictive for production)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=200

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/nextgen_english_prod
# For production, consider using MongoDB Atlas or dedicated MongoDB server

# Database Options
DB_MAX_CONNECTIONS=20
DB_MIN_CONNECTIONS=5

# Email Configuration (for production)
EMAIL_USER=noreply@nextgenenglish.id.vn
EMAIL_PASSWORD=your_email_app_password_here
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587

# Security
BCRYPT_ROUNDS=12
SESSION_SECRET=nextgen_english_session_secret_change_me

# File Upload Limits
MAX_FILE_SIZE=500MB
MAX_VIDEO_SIZE=1GB

# Logging
LOG_LEVEL=info
LOG_FILE=logs/nextgen-english.log